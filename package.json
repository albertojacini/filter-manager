{
  "name": "filter-manager",
  "version": "1.0.0",
  "description": "A manager for HMTL form filters.",
  "main": "src/core.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:albertojacini/filter-manager.git"
  },
  "author": "Alberto Jacini <albertojacini@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "lodash": "^3.10.1"
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "chai": "^3.2.0",
    "del": "^1.2.1",
    "eslint": "^1.1.0",
    "mocha": "^2.2.5"
  },
  "scripts": {
    "lint": "eslint src",
    "test": "mocha test --compilers js:babel/register",
    "build": "babel-node tools/build",
    "start": "babel-node tools/serve",
    "publish": "babel-node tools/build && cd build && git init && git add --all . && git commit --amend --no-edit && git push origin gh-pages -f"
  }
}
